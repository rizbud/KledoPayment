import { connect } from '../../Themes/OsmiProvider'

export default connect({
  container: 'flex items-center px-2 border-b border-gray-500 row justify-between',
  text: 'text-black text-normal py-2 flex bg-white',
  delete: 'items-center justify-center px-3 bg-red-500',
  deleteLabel: 'text-white text-normal'
})
